[project]
name = "astrbot-plugin-blessed-land-runtime"
version = "0.1.1"
description = "Blessed Land Runtime — 小光福地世界运行内核（AstrBot 独立插件，M1.1）"
requires-python = ">=3.11"
dependencies = [
    "SQLAlchemy>=2.0",
    "alembic>=1.13",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[project]
name = "xiaoguang-blessed-land-runtime"
version = "0.1.0-m0"
description = "Blessed Land Runtime — 小光福地世界运行内核（PHASE_1_9_1 M0 Foundation）"
requires-python = ">=3.11"
dependencies = [
    "SQLAlchemy>=2.0",
    "alembic>=1.13",
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
